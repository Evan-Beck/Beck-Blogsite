<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/styles/style.css">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans=serif;
        }
        .navbar {
            background-color: black;
            color: white;
            padding: 10px 0;
            text-align: left;
            height: 25%;
        }
        .navbar a {
            color: white;
            padding: 14px 20px;
            text-decoration: none;
            display: inline-block;
        }
        .navbar a:hover {
            background-color: #ddd;
            color: black;
        }
        .header-button {
            background-color: white;
            color: black;
            border: none;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            margin: 4px 2px;
            cursor: pointer;
            font-size: 20px;
        }
        .content {
            background-color: #f1f1f1;
            padding: 20px;
            height: 75%;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        <a href="/login">Login</a>
        <a href="/logout" style="display: none;">Logout</a>
        <button class="header-button">The Tech Blog</button>
    </div>
    <div class="content">
    {{{body}}}
   </div>   
   
    
</body>
</html>